# Example environment variables for local development.
# Copy to `.env` locally if desired; `.env` is not committed.
# These values are examples only, not production credentials.

DJANGO_SECRET_KEY=insecure-example-change-me
POSTGRES_DB=tezcan_envanter
POSTGRES_USER=tezcan_envanter
POSTGRES_PASSWORD=change-me
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
